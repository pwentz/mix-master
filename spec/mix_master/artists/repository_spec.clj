(ns mix-master.artists.repository-spec
  (:require [speclj.core :refer :all]
            [mix-master.artists.repository :refer :all]))

(describe "create-artist"
  (context "valid fields"
    (it "creates a new artist instanct")))
